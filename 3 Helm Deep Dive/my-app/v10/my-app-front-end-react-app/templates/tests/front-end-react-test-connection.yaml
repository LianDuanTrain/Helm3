apiVersion: v1
kind: Pod
metadata:
  name: {{ include "my-app-front-end-react-app.fullname" . }}-test-connection
  labels:
    {{- include "my-app-front-end-react-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-app-front-end-react-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
